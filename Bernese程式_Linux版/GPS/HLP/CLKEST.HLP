<html>
<body>

<center>
<b>EPOCH-WISE CLOCK INTERPOLATION</b>
<p>Program: CLKEST
</center>
<hr width="100%">

<a name="ENVIRONMENT"   default="%% %%"></a>
<a name="CAMPAIGN"      default=""></a>
<a name="SESSION_TABLE" default="SESSIONS"></a>
<a name="YR4_INFO"      default="$Y+0"></a>
<a name="SES_INFO"      default="$S+0"></a>
<a name="USR_INFO"      default="${USER}"></a>
<a name="CMBINFO"       default=""></a>
<a name="CLKDIFF"       default=""></a>
<a name="RADIO_SUM"     default="1"></a>
<a name="RADIO_SAT"     default="0"></a>
<a name="RADIO_STA"     default="0"></a>
<a name="REFSTA"        default="1"></a>
<a name="REFSAT"        default="1"></a>

<p><b>GENERAL DESCRIPTION</b>
<p>
The program CLKEST does an epoch-wise clock interpolation based on the
carrier phase observations. The main purpose is to get high-rate clock
corrections in an efficient way. Either code observations may be used
to get absolute clock correction values or already available low-rate clock
corrections are fixed and interpolated to a higher sampling.

<p><hr>

<p><b>CLKEST 1: Input Files</b>

<a name="SHOWGEN" default="1"></a>
<p><b>GENERAL FILES</b>
<p><b>Show all general files:</b>
Check this box to get the currently active campaign, session and session table.

<br><br>

<a name="POLE" default=""></a>
<p><b>INPUT</b>
<p><b>Pole file:</b>
This file contains the Earth rotation parameters (ERPs). It is used for
the transformations between the International Celestial Reference Frame
(ICRF) and the International Terrestrial Reference Frame (ITRF). This
file has to be identical to the one used for the computation of the standard
orbit.

<a name="STDORB" default=""></a>
<p><b>Standard orbits:</b>
Select a GNSS standard orbit file. Only one file may be selected. Therefore,
only observations covered by the standard orbit may be processed.

<a name="SATCLK" default=""></a>
<p><b>Satellite clocks:</b>
Select the satellite clock file, which contains the satellite clock
corrections to be used for the clock interpolation.

<p>Example: <a href="/$X/DOC/EXAMPLE1.CLK">Example</a>

<a name="CLKRNX" default=""></a>
<p><b>Clock RINEX file:</b>
Select the clock RINEX file corresponding to the satellite clock file
chosen above. This file should also contain the receiver clock corrections
for the epoch-wise clock interpolation of the receiver clocks.

<p>Example: <a href="/$X/DOC/EXAMPLE3.CLK">Example</a>

<a name="DCBINP" default=""></a>
<p><b>Code bias inp. file:</b>
Selection of a code bias input file containing differential
(P1-P2 or P1-C1) code biases of satellites (and receivers) is
generally recommended. These biases are irrelevant only when
processing the L3 linear combination of code measurements from
P1/P2 (not C1/X2 or C1/P2) receivers.

<p>Example: <a href="/$X/DOC/EXAMPLE.DCB">Example</a>

<a name="COORD" default=""></a>
<p><b>A priori coordinates:</b>
Selection of an a priori coordinate file. This coordinate file must
contain all the sites you want to process.

<p>Example: <a href="/$X/DOC/EXAMPLE.CRD">Example</a>

<a name="TROPEST" default=""></a>
<p><b>Est.troposphere val.:</b>
Here a troposphere estimates file from <a href="PGMLST.HLP#GPSEST">GPSEST</a>
or <a href="PGMLST.HLP#ADDNEQ2">ADDNEQ2</a> can be
selected.

<p>Example: <a href="/$X/DOC/EXAMPLE.TRP">Example</a>

<p><i>Default value:</i> blank

<a name="VMF1_FILES" default=""></a>
<p><b>Maps of VMF1 coeff.:</b>
For the continued fraction of the troposphere mapping function, the
Vienna Mapping Functions 1 (VMF1, <a href="REFERENCES.HLP#Boehm_etal_2006">
B&ouml;hm et al., 2006</a>) rely on global grids of the a, b, and c coefficients,
partly based on the ray-tracing of the European Centre for Medium-Range
Weather Forecasts (ECMWF) numerical weather models output. For more details
on the VMF1 or to download VMF1 grids, please visit the
<a href="LINKS.HLP#VMF1">VMF1 website</a>.

<a name="OCNLOAD" default=""></a>
<p><b>Ocean loading corr.:</b>
Optionally a file containing the ocean loading amplitudes and phases may be
specified. The format is the de facto IERS standard.
<br>
The file becomes mandatory if you use a Standard orbit with CMC applied.
Be sure to select the same file as for the programs used to generate the
orbit file.

<p><i>Note:</i>:
For instructions on how to get ocean loading coefficients
for a set of (new) points please refer to
<a href="/$X/DOC/README_BLQ.TXT">IGS Mail 3521</a>.

<p><a href="/$X/DOC/EXAMPLE.BLQ">Example</a>

<a name="ATMLOAD" default=""></a>
<p><b>Atmosph. load. corr.:</b>
Optionally a file containing atmospheric tidal loading corrections may be
specified.
<br>
The file becomes mandatory if you use a Standard orbit with CMC applied.
Be sure to select the same file as for the programs used to generate the
orbit file.

<p><i>Remark:</i>
This file can be generated by the program
<a href="PGMLST.HLP#GRDS1S2">GRDS1S2</a>

<p><a href="/$X/DOC/EXAMPLE.ATL">Example</a>

<br><br>

<a name="OBSFIL" default=""></a>
<p><b>INPUT FILES</b>
<p><b>Observation files:</b>
Selection of zero-difference code files to be processed.

<p><i>Note:</i>:
The selection list is only for code observation files. Nevertheless, code
as well as phase observation files will be used during the program
run. Therefore, please make sure to have the corresponding phase observation
files available as well.

<p><i>Default value:</i> <tt>????$S+0</tt>

<p><hr>

<p><b>CLKEST 1.1: General Files</b>

<a name="CONST" default="CONST."></a>
<p><b>GENERAL INPUT FILES</b>
<p><b>General constants:</b>
This file contains all the physical and astronomical constants used
in the Bernese GNSS Software. Most probably you will never have to
modify this file.

<p>Example: <a href="/$X/GEN/CONST.">CONST.</a>

<a name="SUBMOD" default="IERS2010XY"></a>
<p><b>Subdaily pole model:</b>
This file contains the parameters for the subdaily pole model. It
has to be identical to the one used for the computation of the
standard orbit.

<p>Example: <a href="/$X/GEN/IERS2010XY.SUB">IERS2010XY.SUB</a>

<a name="NUTMOD" default="IAU2000R06"></a>
<p><b>Nutation model:</b>
This file contains the parameters for the nutation model. It
has to be identical to the one used for the computation of the
standard orbit.

<p>Example: <a href="/$X/GEN/IAU2000R06.NUT">IAU2000R06.NUT</a>

<a name="SATCRUX" default="SAT_$Y+0"></a>
<p><b>Satellite problems:</b>
This file contains information on satellite problems concerning modeling.
It is used for exclusion of misbehaving satellites
(and for orbit arc splitting).
It may also contain (mean) epochs of GPS repositioning events.

<p><i>Note:</i>
Corresponding year-specific files are maintained by AIUB/CODE.
You can get the latest version of the current file from the
<a href="/$X/DOC/README_AIUB_AFTP.TXT">anonymous ftp</a> account of AIUB.

<p>Example: <a href="/$X/GEN/SAT_2011.CRX">$X/GEN/SAT_2011.CRX</a>

<p><i>Default value:</i> <tt>SAT_$Y+0</tt>

<a name="SATELL" default="SATELLIT.I08"></a>
<p><b>Satellite information:</b>
This file contains general information on GNSS (and LEO/GEO) satellites.
For the session to be processed,
each satellite has to appear once in this file
within a corresponding time window.

<p><i>Note:</i>
The file <a href="/$X/GEN/SATELLIT.I08">$X/GEN/SATELLIT.I08</a> is maintained
by AIUB/CODE.
It includes a complete history concerning the GPS satellite constellation
(and newly launched satellites are regularly added).
You can get the latest version of this file from the
<a href="/$X/DOC/README_AIUB_AFTP.TXT">anonymous ftp</a> account of AIUB.

<p>Example: <a href="/$X/GEN/SATELLIT.I08">$X/GEN/SATELLIT.I08</a>

<a name="RECEIVR" default="RECEIVER."></a>
<p><b>Receiver information:</b>
This file contains in essence the code measurement types for a list of
receivers. These specifications are relevant when considering so-called
differential (P1-P2 or P1-C1) code biases.

<p>Example: <a href="/$X/GEN/RECEIVER.">$X/GEN/RECEIVER.</a>

<a name="DATUM" default="DATUM."></a>
<p><b>Geodetic datum:</b>
This file contains the definition of the geodetic datum and of the
ellipsoid to be used to compute ellipsoidal coordinates. It is used
to apply antenna heights and for the printing of coordinates in the
local system (north, east, up). You only have to modify this file
when you want to introduce a new reference ellipsoid.

<p><i>Caution:</i>
This file has no effect on Cartesian coordinates.

<p>Example: <a href="/$X/GEN/DATUM.">$X/GEN/DATUM.</a>

<a name="PHASECC" default="PCV.I08"></a>
<p><b>Phase center offsets:</b>
This file contains receiver antenna phase center offsets and optionally
elevation-dependent phase center patterns. In addition, the file contains
satellite-specific antenna phase center patterns (dependent on nadir angle)
as well as offset values. These satellite antenna offset values are zero as
long as no frequency dependent offsets for the satellite transmitter
antennas are known. The frequency independent offset values of the
transmitter antennas are given in the corresponding satellite information
file <a href="/$X/GEN/SATELLIT.I08">$X/GEN/SATELLIT.I08</a> and are applied
from there.

<br>
You have to modify this file when you use a new antenna type
or when you wish to consider your own PCV calibrations.
The program <a href="PGMLST.HLP#ATX2PCV">ATX2PCV</a> allows to convert
PCV information given in ANTEX format into Bernese-formatted PCV tables.

<p><i>Important note:</i>
The file $X/GEN/PCV.I08 contains absolute PCV values
for most of the receiver antenna types commonly used
within IGS (and EUREF).

<p>Example: <a href="/$X/GEN/PCV.I08">$X/GEN/PCV.I08</a>.

<a name="GPSUTC" default="GPSUTC."></a>
<p><b>GPS-UTC file:</b>
Specify the file with GPS-UTC values here. You have to update this file
each time a leap second is introduced.

<p>Example: <a href="/$X/GEN/GPSUTC.">GPSUTC.</a>

<p><i>Default value:</i> <tt>GPSUTC.</tt>

<p><hr>

<p><b>CLKEST 2: Output Files</b>

<a name="CLKINF" default=""></a>
<p><b>OUTPUT FILES</b>
<p><b>Clock RINEX file:</b>
Name of the resulting RINEX clock file.

<a name="CLKRES" default=""></a>
<p><b>Satellite clocks:</b>
Name of the resulting satellite clock file in Bernese format.

<a name="EPOCHS" default=""></a>
<p><b>Missing epochs:</b>
Name of file with a list of missing satellite clock epochs.

<br><br>

<a name="SYSODEF" default="1"></a>
<a name="SYSOUT"  default="CLKEST"></a>
<p><b>GENERAL OUTPUT FILES</b>
<p><b>Program output:</b>
You can choose to have the program output written to a separate file for each
program run (checkbox marked), or specify a common name for all program runs.
<br>
If you mark the checkbox, the filename will correspond to the program name
with an extension containing a counter (Lnn), which is automatically
incremented for each program run. If the counter reaches the maximum value, it
is automatically reset, and existing files will be overwritten.
The maximum value can be set in
<b>Menu &gt; Configure &gt; Program Names</b>, option "Maximum program output
file number".
<br>
If the checkbox is unmarked, you have to specify a name for the program
output file. This allows to characterize the program run, e.g., by using the
day and year (<tt>$YD+0</tt>) in the filename.

<a name="ERRMRG" default="0"></a>
<a name="SYSERR" default="ERROR"></a>
<p><b>Error messages:</b>
You can choose to have error messages written into the program's output
file (see above), or specify a separate file to which error messages are
written.
<p><hr>

<p><b>CLKEST 3.1: Options</b>

<a name="TITLE" default=""></a>
<p><b>TITLE</b>
<p>
This title line will be printed as header comment into the program output
to document the program run. The title should characterize the program run by,
e.g., giving the most important options used and the session.

<br><br>

<a name="MAXZEN" default="80"></a>
<p><b>OBSERVATION SELECTION</b>
<p><b>Max zenith distance:</b>
Only observations down to the maximum zenith distance specified
here will be used.

<p><i>Default value:</i> <tt>80</tt> degrees

<a name="USEWIN" default="1"></a>
<p><b>Use observation window:</b>
Usually all observations in the observation files are processed.
Mark this checkbox to enter a time window for the observations to be processed.

<a name="SAMPLING" default="30"></a>
<p><b>Sampling of data:</b>
Specify the sampling for the resulting clock corrections. This sampling has
to be available in the selected observation files.

<a name="DTSIM" default="0.1"></a>
<p><b>Max difference of equal epochs:</b>
The epoch-wise processing is done for all the observations that
are 'simultaneous', where simultaneous is defined by the time
interval specified here. If you are processing 1 sec data you better
change the time interval to a fraction of a second, else observations
that belong to different epochs might be processed together and thus
correlated.

<p><i>Default value:</i> <tt>0.1</tt> seconds


<a name="SECIPL" default="300"></a>
<p><b>Max clock interpolation interval:</b>
The interpolation of the satellite clocks is mandatory, because the input
clock corrections have a lower sampling (e.g. 300&nbsp;sec) than the
resulting clock corrections (e.g. 30&nbsp;sec). In order to use meaningful a
priori values for the clock corrections specify here at least the sampling
of the input clock corrections.

<p><i>Default value:</i> <tt>300</tt>

<a name="LINCOMB" default="L3"></a>
<p><b>Linear combination:</b>
Specify the linear combination to be used for the epoch-wise clock
interpolation. Recommended is the ionosphere-free (L3) linear combination,
because input clock products are in most cases consistent with this linear
combination.

<a name="MINSTAT" default="3"></a>
<p><b>Min number of stations observing one satellite:</b>
A satellite clock correction is only estimated if at least this number of
stations are observing this specific satellite for the processed
epoch.

<a name="PRELJ2" default="0"></a>
<p><b>Periodic relativistic J2-correction:</b>
Check this checkbox to apply the periodic relativistic J2-correction (see
<a href="REFERENCES.HLP#Kouba2004">Kouba (2004)</a>.
<br>
Note that the IGS is <i>not</i> applying this correction but it is
necessary if you have modeled the satellite clocks in GPSEST with a
low-order polynomial model.

<p><i>Default value:</i> unchecked

<p><hr>

<p><b>CLKEST 3.2: Observation Window</b>

<a name="RADIO_1" default="1"></a>
<a name="RADIO_2" default="0"></a>
<p><b>OBSERVATION WINDOW</b>
<p>
This panel allows to specify a time window. Only observations
within this time window will be used for processing.
<br>
You may define the time window either by using session definitions, or by
explicitly specifying start and end times.

<a name="SESSION_YEAR" default="$Y+0"></a>
<a name="SESSION_STRG" default="$S+0"></a>
<p><b>Year and Session identifier:</b>
You may specify one session from the <a href="SESSIONS.HLP">session table</a>.
The boundaries of this session define the observation window. If you use an
open session table the year has to be specified in addition to the session
identifier. If you use a fixed session table the year is read from the session
definition and the value in the panel is ignored.
<br>
It is possible to specify a range of sessions using the corresponding
menu variable (<tt>$S+-</tt>). In this case the time window starts at the
beginning of the first session of the range and stops at the end of the last
session of the range. If you use an open session table the year should also
be given with the range variable (<tt>$Y+-</tt>).

<p><i>Default values:</i> Year: <tt>$Y+0</tt> session: <tt>$S+0</tt>

<a name="STADAT" default="$YMD_STR+0"></a>
<a name="STATIM" default="00 00 00"></a>
<a name="ENDDAT" default="$YMD_STR+0"></a>
<a name="ENDTIM" default="23 59 59"></a>
<p><b>Start and End times:</b>
Specify date and time for the begin and the end of the observation window.
<br>
To specify a time window which is open on one side of the interval,
leave the corresponding input field for the start/end date empty. The
associated start/end time is then ignored.

<p><i>Default values for daily processing:</i> Start date: <tt>$YMD_STR+0</tt>,
start time: 00&nbsp;00&nbsp;00, end date: <tt>$YMD_STR+0</tt>,
end time: 23&nbsp;59&nbsp;59

<p><hr>

<p><b>CLKEST 3.3: Options</b>

<a name="TROPOS" default="GMF"></a>
<p><b>TROPOSPHERE</b>
<p><b>Troposphere model:</b>
Specify the troposphere model to be used for the processing. Note that the
same model should be used as in the file with
the <a href="#TROPEST">Est.troposphere val.</a>. If you use a different
model and do not check the next option <a href="#CHKTROP">Remove obs with
missing trp</a> your processing is inconsistent. Your results are then
meaningless!

<a name="CHKTROP" default="1"></a>
<p><b>Remove observations with missing troposphere:</b>
If this box is checked, only observations which have a
corresponding troposphere estimate available in the troposphere file
specified in <a href="#TROPEST">Est.troposphere val.</a> are used.
<br><br>

<a name="CMBSEL" default="TRIDIAG"></a>
<p><b>CLOCK COMBINATION</b>
<p><b>Combination algorithm:</b>
The TRIDIAG algorithm has to be used if you want to epoch-wise
interpolate already available precise low-rate clock corrections. If you
want to generate high-rate clock corrections without fixing them to precise
low-rate clock corrections, you may also use the SUM_CONDITION algorithm.
<br>
TRIDIAG algorithm: The normal equation matrix, which has to be solved for
the clock combination is a tridiagonal
matrix (<a href="REFERENCES.HLP#Bock_etal_2009">Bock et al, 2009</a>). An efficient
solution algorithm (<a href="REFERENCES.HLP#Press1996">Press et al,
1996</a>) is used to solve such a equation system.
<br>
SUM_CONDITION algorithm: The sum of all estimated clock corrections has to
be zero.


<a name="USECODE" default="0"></a>
<p><b>Use code observations:</b>
The code observations are internally used for the clock synchronization,
but depending on this option they will also be used for the final clock
results or not. If you use precise low-rate clock corrections as input and
fix the clock results on these a priori values (see
option <a href="#IFIX">Fix on a priori clocks</a>) then the code observations
should not be used.

<a name="IFIX" default="1"></a>
<p><b>Fix on a priori clocks:</b>
If this box is checked the resulting clocks are fixed on the a priori
values from the input clock file (see <a href="#CLKRNX">Clock RINEX
file</a>). This is only recommended if the a priori clock values are
already the result of a precise clock estimation.


<a name="REMOVE" default="1"></a>
<p><b>Remove non-fixed data pieces:</b>
If option <a href="#IFIX">Fix on a priori clocks</a> is checked, it makes
sense to remove data pieces which cannot be fixed due to missing a priori
clocks.

<a name="DTFIX" default="10"></a>
<p><b>Remove clocks if interval to tfix is larger than:</b>
Clock corrections which are more than the specified number of minutes away
from the next fixed clock correction are removed from the resulting
clock. This number depends on the sampling of the clocks to be fixed on. As
a rule of thumb the recommendation is the double interval of this sampling,
i.e. 5 min sampling of the a priori clocks means 10 minutes for this option
here.

<a name="ALLIGN" default="1"></a>
<p><b>Align with a priori clocks:</b>
Alignment with a priori clocks is recommended, because otherwise you may
run into numerical instabilities in the processing.

<br>

<p><hr>

<p><b>CLKEST 4: Screening Options</b>

<a name="OMCMXC" default="100000."></a>
<a name="OMCMAX" default="100.00"></a>
<p><b>OPTIONS FOR PROCESSING</b>
<p><b>Max tolerated term observed-minus-computed:</b>
This value is one limit for the pre-processing of the observations. If
the value of observed-minus-computed for the processed observation exceeds
the given value, it is not further used in the processing.

<p><i>Default value:</i> <tt>100000.0</tt> meters for code observations
<p><i>Default value:</i> <tt>100.0</tt> meters for phase observations

<a name="APRSIC" default="0.5"></a>
<a name="APRSIP" default="0.002"></a>
<p><b>A priori sigma of zenith observation:</b>
In order to preprocess the observations in a realistic manner, an a priori
sigma has to be specified for the processing.

<p><i>Default value:</i> <tt>0.5</tt> meter for code observations
<p><i>Default value:</i> <tt>0.002</tt> meter for phase observations

<a name="RESMXC" default="0.8"></a>
<a name="RESMXP" default="0.01"></a>
<p><b>Max clock residual tolerated:</b>
Specify a maximum value for a residual to be accepted. If the residual
exceeds this value the corresponding observations are not further used
in the processing.

<p><i>Default value:</i> <tt>0.8</tt> meter for code observations
<p><i>Default value:</i> <tt>0.01</tt> meter for phase observations

<a name="RMSMXC" default="0.1"></a>
<a name="RMSMXP" default="0.001"></a>
<p><b>Max RMS tolerated for clock estimation:</b>
The RMS for the clock estimation must not exceed the value specified
here. If this is still the case in the last iteration
(see <a href="#MAXITC">Max. number of iterations if RMS exceeded</a>
and <a href="#MAXITP"></a> ) the clock results for this epoch are not used.

<p><i>Default value:</i> <tt>0.1</tt> meter for code observations
<p><i>Default value:</i> <tt>0.001</tt> meter for phase observations

<a name="MAXITC" default="3"></a>
<a name="MAXITP" default="4"></a>
<p><b>Max number of iterations if RMS exceeded:</b>
The clock estimation is done iteratively. This value gives the maximum
number of allowed iterations.

<p><i>Default value:</i> <tt>3</tt> iterations for code observations
<p><i>Default value:</i> <tt>4</tt> iterations for phase observations

<br><br>

<p><hr>

<p><b>CLKEST 5.1: Write RINEX Clock File</b>

<a name="RADIO_ALL" default="1"></a>
<p><b>WRITE STATION CLOCKS</b>
<p><b>Write all station clocks:</b>
All available station clocks are written into the output RINEX clock file.

<a name="RADIO_SPC" default="0"></a>
<p><b>Write clocks for stations in special list:</b>
Only the station clocks from the special list (<a href="#STALIST">Stations</a>) are
written into the output RINEX clock file.

<a name="RADIO_FIX" default="0"></a>
<p><b>Write station clocks at fixing epochs:</b>
The station clocks are only written at the fixing epochs into the output
RINEX clock file.

<a name="RADIO_NOT" default="0"></a>
<p><b>Write no station clocks (except reference):</b>
The station clocks are not written into the output RINEX clock file except
if the reference clock is a station clock.

<a name="COPYRNX" default="1"></a>
<p><b>Copy clocks from input RINEX file to output:</b>
The clocks from the input RINEX file are copied to the output RINEX
file. This option makes only sense if you are fixing (<a href="#IFIX">Fix
on a priori clocks</a>) the clock results on the input clock values.

<a name="MISSRNX" default="0"></a>
<p><b>Do not write clocks missing in input RINEX file at fixing epochs:</b>
It may happen that some clocks are missing in the input clock file at the
fixing epochs. If you check the box of this option you prevent that the
estimated clock for this epoch is written to the output file. A mix of
fixed and estimated clocks at an epoch is avoided this way.

<br><br>

<a name="RUNBY" default=""></a>
<p><b>RINEX HEADER INFORMATION</b>
<p><b>Run by:</b> Enter the name of the operator of this program run.

<a name="AC" default=""></a>
<p><b>AC:</b> Enter the acronym of the analysis center here.

<a name="ACNAME" default=""></a>
<p><b>AC name:</b> Enter the name of the analysis center here.

<a name="TIMESYS" default="GPS"></a>
<p><b>Time system:</b>
Content of TIME SYSTEM in the header of the CLOCK RINEX output file.

<a name="DCBLINE" default=""></a>
<p><b>DCBS APPLIED:</b>
Last 40 characters of DCBS APPLIED in the header of the CLOCK RINEX output
file.

<a name="COMMENT" default=""></a>
<p><b>Comment:</b>
Enter any comment lines here.

<p><hr>

<p><b>CLKEST 5.2: List of Clocks for Output Clock RINEX File</b>

<a name="STALIST" default=""></a>
<p><b>Stations:</b>
Select the stations from which clocks should be written to the output clock
RINEX file.

<p><hr>

<p><b>CLKEST 5.3: Reference Clock for Clock Files</b>

<a name="RADIO_RXC" default="1"></a>
<p><b>REFERENCE CLOCK STATION</b>
<p><b>Reference clock from Clock RINEX file:</b>
The reference clock from the input clock RINEX file is taken. Please be
sure that this clock is also present in your processing. Otherwise the
reference clock is selected automatically.

<a name="RADIO_AUTO" default="0"></a>
<p><b>Reference clock automatically:</b>
The reference clock is selected automatically from the processed data.

<a name="RADIO_RFIL" default="0"></a>
<a name="REFSIG"     default=""></a>
<p><b>Reference clock from file:</b>
The reference clock is selected from a list in the specified file.

<a name="RADIO_RLST" default="0"></a>
<p><b>Reference clock from list:</b>
The reference clock is selected from a station list
(<a href="#REFLIST">Stations</a>).

<a name="RADIO_RSTA" default="0"></a>
<a name="REFCLK" default=""></a>
<p><b>Reference clock name:</b>
A clock name can be specified, which should be selected as reference
clock. Please make sure in this case that this clock is really present in our
processing.

<p><hr>

<p><b>CLKEST 5.4: List of Reference Clocks for Output Clock File</b>

<a name="REFLIST" default=""></a>
<p><b>Stations:</b>
Select a list of stations from which the reference clock should be selected.

<p><hr>

<p><b>CLKEST 5.5: Write Bernese Clock File</b>

<a name="TITCLK" default=""></a>
<p><b>TITLE OF FILE:</b>
Specify the title, which will be written in the title line of the Bernese
satellite clock file (<a href="#CLKRES">Satellite Clock File (Bernese
Format)</a>).

<p><hr>

</body>
</html>
